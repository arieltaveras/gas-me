import React from "react";

const ZIP_LENGTH = 5;

function ZipSearch({zip}) {
    return (
        <div>
            <input 
            placeholder="Search by zip"/>
        </div>
    )
};

export default ZipSearch;